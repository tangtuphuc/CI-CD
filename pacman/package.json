{
  "name": "my-html-app",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js"
    "build": "npm run lint && npm run build:html && npm run build:css",
    "build:html": "node build-html.js",
    "build:css": "node build-css.js",
    "lint": "html-validate . && stylelint .",
  },
   "devDependencies": {
    "html-validate-action": "^1.3.1",
    "stylelint-github-actions": "^3.0.0",
    "html-validate": "^5.4.0",
    "stylelint": "^14.0.0"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
